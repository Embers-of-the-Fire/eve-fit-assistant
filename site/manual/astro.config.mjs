// @ts-check
import { readFileSync } from "node:fs";
import starlight from "@astrojs/starlight";
import { defineConfig } from "astro/config";

// Generated by `./x build site-manual` from docs/manual, docs/changelog, docs/announcements.
const sidebar = JSON.parse(
    readFileSync(new URL("./src/generated/sidebar.json", import.meta.url), "utf-8"),
);

// https://astro.build/config
export default defineConfig({
    integrations: [
        starlight({
            title: "EVE Fit Assistant",
            defaultLocale: "root",
            locales: {
                root: { label: "English", lang: "en" },
                zh: { label: "简体中文", lang: "zh-CN" },
            },
            social: [
                {
                    icon: "github",
                    label: "GitHub",
                    href: "https://github.com/Embers-of-the-Fire/eve-fit-assistant",
                },
            ],
            sidebar,
        }),
    ],
});
