name: efa_proto
description: "Generated Dart protobuf bindings for the EFA data schemas in data/schema/."
publish_to: none
resolution: workspace
version: 0.1.0

environment:
  sdk: ^3.12.0

dependencies:
  protobuf: ^6.0.0

dev_dependencies:
  flutter_lints: ^6.0.0
