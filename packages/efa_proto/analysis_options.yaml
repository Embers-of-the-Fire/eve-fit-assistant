include: package:flutter_lints/flutter.yaml

# Everything under lib/ is protoc-gen-dart output (gitignored, regenerated by
# `./x generate protobuf`); never analyze or lint it.
analyzer:
  exclude:
    - lib/**
