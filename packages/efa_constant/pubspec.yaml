name: efa_constant
description: "EVE Online static constant definitions (category/group/attribute IDs, dogma units)."
publish_to: none
resolution: workspace
version: 0.1.0

environment:
  sdk: ^3.12.0

dev_dependencies:
  flutter_lints: ^6.0.0
