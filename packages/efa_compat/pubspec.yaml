name: efa_compat
description: "Platform compatibility shims for dart:io, dart:isolate and web WASM preconditions."
publish_to: none
resolution: workspace
version: 0.1.0

environment:
  sdk: ^3.12.0

dependencies:
  flutter:
    sdk: flutter
  web: ^1.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
