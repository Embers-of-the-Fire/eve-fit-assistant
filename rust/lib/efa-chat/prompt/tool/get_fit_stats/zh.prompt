通过运行完整装配引擎来计算挂载装配的核心数据。需要本会话已挂载装配；若当前没有挂载，请先让用户打开一个装配，或用 `load_fit` 加载一个。包含以下部分：伤害（含/不含换弹的 DPS、齐射、单发、无人机/舰载机 DPS）、防御（各层 EHP、原始 HP、有效抗性）、电容（容量、峰值回充/负载/差值、耗尽时间（秒）——负值或极大值表示稳定）、机动（速度、对齐时间、质量、曲速速度）、装配资源（CPU/能量栅格的输出与剩余）以及锁定（最大锁定目标数、扫描分辨率、信号半径）。如需更具体的信息，请使用 `get_item` 或 `get_attr`。
