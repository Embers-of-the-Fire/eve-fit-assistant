Inspect one calculated item of the attached fit with every attribute (id, name, base value, computed value). `item_type` is one of: hull (the ship itself), module (fitted high/medium/low/rig/subsystem modules), drone, fighter, implant, booster, character. `index` selects the item within that section (0-based, in fitted order) and is required for all sections except hull and character.
