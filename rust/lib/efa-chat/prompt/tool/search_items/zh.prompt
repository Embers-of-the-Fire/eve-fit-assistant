按（本地化）名称子串搜索游戏物品数据库。传入与用户所提名称语言一致的 `language`（例如 "en" 或 "zh"）可搜索对应的本地化版本；省略则使用应用的显示语言。已知目标物品类别时传入 `kind`（"ship"、"module"、"charge"、"drone"、"fighter"、"implant" 或 "booster"）可排除无关匹配。最多返回 20 个匹配项，包含 `type_id`、`name`、`group_id` 和 `category_id`；植入体与增效剂还带有 `slot_index`（传给 `propose_fit_edit` 的槽位号）与 `slot_kind`（"implant" 或 "booster"——两者同属 category 20，请用 `kind` 区分）。当用户按名称提及某物品时，请先用此工具解析后再进行推理；category 6 为舰船、7 为模块、8 为装药、18 为无人机、20 为植入体/增效剂、87 为舰载机、22 为可部署物。