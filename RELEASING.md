# Releasing

This document describes the app release procedure for EVE Fit Assistant. App releases are fully automated through GitHub Actions once the release pull request is merged.

## Overview

- App releases target the `dev` branch only.
- A release is driven by a pull request with three labels: `V-Release`, `V-Test`, and `V-Tested Release`.
- The merge commit is the release point; no manual tag creation is required.
- The canonical version lives in `efa.config.toml` under `[version]`. `pubspec.yaml`, `rust/Cargo.toml`, and `pyproject.toml` are derived from it.

## 1. Prepare the release locally

### 1.1. Bump the version

Edit `efa.config.toml` under `[version]`. Example:

```toml
[version]
major = 0
minor = 1
patch = 0
pre_label = "beta"
pre_num = 7
build = 7
```

> The fitting-engine submodule at `rust/lib/eve-fit-os` has independent versioning and is **not** part of the sync targets.

### 1.2. Generate the release note

```bash
./x release relnote
```

This creates `docs/changelog/<version-dir>/` containing:

- `spec.yaml` — release metadata (channels, platforms, app version, etc.).
- `changelog.md` — changelog body generated by `git-cliff` from conventional commits.

You must author the localized content files separately:

- `content.zh.md`
- `content.en.md`

For version `0.1.0-beta.7`, the directory is `docs/changelog/0-1-0-beta-7/`.

When the release PR is merged, the release workflow automatically stages the
release note as a remote announcement entry and publishes it alongside the
release channel data.

To override the version or published time:

```bash
./x release relnote --version 0.1.0-beta.7 --published-at 2026-07-16T12:00:00Z
```

Use `--force` to overwrite an existing release note directory.

### 1.3. Sync the version to package manifests

```bash
./x release version sync
```

This updates:

- `pubspec.yaml` — Flutter app version (`version: <full>`).
- `rust/Cargo.toml` — bridge crate version (`version = "<semver>"`).
- `pyproject.toml` — Python package version (`version = "<semver>"`).

Use `--dry-run` to preview changes without writing files.

### 1.4. Run local preflight checks

```bash
./x ci release verify --check-all
```

This verifies:

- Version targets match `efa.config.toml`.
- The expected release tag does not already exist.
- Release notes exist and are valid.
- Submodules are initialized and clean.
- Generated code is up to date.
- The project builds statically (`flutter analyze`).
- Python and Flutter tests pass.

For a faster check that matches CI:

```bash
./x ci release verify --base-ref origin/dev --check-notes --check-tag --check-note-content
```

### 1.5. Lint and commit

```bash
./x lint
```

Stage and commit all release changes, including:

- `efa.config.toml`
- `pubspec.yaml`
- `rust/Cargo.toml`
- `pyproject.toml`
- `docs/changelog/<version-dir>/`
- Any generated files updated by `./x lint` or `./x generate -f all`.

## 2. Open the release PR

Open a pull request targeting `dev` and add the `V-Release` label.

This triggers `release-preflight.yml`, which runs fast checks:

```bash
./x ci release verify --base-ref origin/dev --check-notes --check-tag --check-note-content
```

A force-push or new commit removes `V-Test` and `V-Tested Release` labels so the release must be re-tested.

## 3. Trigger full release tests

When the PR is ready, add the `V-Test` label.

This triggers `release-full.yml`, which runs:

- `App release test` — builds the APK in test mode using `_release.yml`.
- `Data release test` — builds data snapshots in test mode using `_release-data.yml`.
- `Mark as tested release` — adds `V-Tested Release` and removes `V-Test` after both jobs succeed.

The test jobs only run if `V-Release` and `V-Test` labels are present and the PR is from the upstream repository.

## 4. Merge the release PR

Once both `V-Release` and `V-Tested Release` are present, merge the PR into `dev`.

The `release.yml` workflow on the merge commit then:

1. Identifies the merged PR and confirms it has `V-Tested Release`.
2. Reuses `_release.yml` to build and publish the APK.
3. Merges the release registry fragment.
4. Publishes the release to the remote `testing` channel.
5. Publishes the release note as a remote announcement entry so users are notified
   of the new version.
6. Creates a lightweight Git tag (`v<version>`) pointing at the merge commit.

If the merged PR is missing `V-Tested Release`, the release aborts.

## Quick reference

| Step | Command / Action |
|------|------------------|
| Bump version | Edit `efa.config.toml` `[version]` |
| Generate release note | `./x release relnote` |
| Author localized content | `docs/changelog/<version-dir>/content.{zh,en}.md` |
| Sync version targets | `./x release version sync` |
| Full local preflight | `./x ci release verify --check-all` |
| Fast local preflight | `./x ci release verify --base-ref origin/dev --check-notes --check-tag --check-note-content` |
| Format and lint | `./x lint` |
| Open release PR | Target `dev` + label `V-Release` |
| Trigger full tests | Add label `V-Test` |
| Merge | Requires `V-Release` and `V-Tested Release` |

## Release labels

| Label | Meaning |
|-------|---------|
| `V-Release` | Marks the PR as a release candidate; triggers preflight checks. |
| `V-Test` | Requests the full release test build. |
| `V-Tested Release` | Added automatically after full tests pass; required to merge. |

## Notes

- Do not create the release tag manually. The tag is created by `release.yml` after the merge.
- A force-push or new commit on the PR drops `V-Test` and `V-Tested Release`; re-add `V-Test` to re-test.
- The `release-preflight.yml` fast checks use `--base-ref origin/dev` to ensure the version is greater than the one on the base branch.
