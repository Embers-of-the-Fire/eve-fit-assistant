# Remote mock maintenance notice

This mock announcement is served from the local remote origin. Use it to verify that the object layout, channel index, and document body paths are reachable through static HTTP or MinIO.

The mock does not require application credentials and should not be used as production content.
